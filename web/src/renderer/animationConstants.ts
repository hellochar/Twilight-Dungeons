/** Unity SleepTaskController: deep sleep tints the actor sprite blue. */
export const DEEP_SLEEP_TINT = 0x5DABFF; // Color(0.365, 0.6712619, 1)

/** GrowAtStart ANIMATION_TIME (Unity): total duration of spawn scale-in animation (seconds). */
export const SPAWN_ANIMATION_DURATION = 3.0;

/** FadeThenDestroy (Unity FloorController): duration in milliseconds. */
export const FADE_DURATION_MS = 500;

/** FadeThenDestroy (Unity FloorController): final scale factor at end of fade (50%). */
export const FADE_END_SCALE = 0.5;

/** TelegraphedTask.prefab fade-out duration (seconds). */
export const TELEGRAPH_FADE_DURATION = 0.25;
