# Twilight Dungeons Web Port — Progress

Always begin by reading the top-level implementation plan `web\web-port-implementation-plan.md`.
There's also the larger context doc: `web-puzzle-port.md`.
Stay as close as possible to the Unity version. Always refer back to it for sprites, colors, behavior, look and feel, logic, sizing and layout, and all aspects of the game.

**Terminology:** "Sprints" here are actual work sittings (one conversation).
"Sessions" in the implementation plan are logical milestones. They diverged after sprint 6.

## Sprint 1 (2026-02-25): Project Scaffold + Core Types ✅
*Plan sessions 1*

### Completed Files
- [x] Project scaffold (Vite + React + TypeScript)
- [x] Dependencies: pixi.js@^8.16, gsap, @pixi/particle-emitter
- [x] src/core/Vector2Int.ts — static methods, directional constants, key() for map usage
- [x] src/core/MyRandom.ts — mulberry32 seeded PRNG, Shuffle, Pick helpers
- [x] src/core/Modifiers.ts — symbol-tagged modifier chain (8 modifier types)
- [x] src/core/types.ts — Faction, TileVisibility, CollisionLayer (bitflags), ActionType enums + handler interfaces
- [x] src/core/EventEmitter.ts — typed event emitter
- [x] src/core/index.ts — barrel export
- [x] Directory structure: core/, model/, model/tasks/, model/enemies/, model/grasses/, model/items/, model/statuses/, generator/, daily/, renderer/, ui/, input/, hooks/, scripts/

### Design Decisions
- Vector2Int: immutable (readonly x, y), static methods for operations, `key()` for Map keys
- MyRandom: module-level singleton (like C# static class), mulberry32 PRNG
- Modifiers: symbol tags on interfaces (ACTION_COST_MOD, etc.), collectModifiers() recurses into sub-providers
- CollisionLayer: numeric enum with bitwise ops (matches C# [Flags])
- Handler interfaces (IDeathHandler, IActorEnterHandler, etc.): use `any` for forward-declared types to avoid circular deps

## Sprint 2 (2026-02-25): Entity + Tile + Floor ✅
*Plan session 2*

### Completed Files
- [x] src/model/GameModelRef.ts — singleton ref to break circular deps (Entity↔GameModel↔Floor)
- [x] src/model/Entity.ts — abstract base: guid, floor, pos, isDead, timedEvents, modifier chain, kill/death system, TimedEvent, NoSpaceException
- [x] src/model/Tile.ts — abstract Tile + Ground, Wall, Chasm, HardGround, FancyGround, Signpost, Soil, Water. Skipped Upstairs/Downstairs (single-floor daily puzzle)
- [x] src/model/Floor.ts — StaticEntityGrid<T>, MovingEntityList<T>, ISteppable, Floor class with: entity stores, put/remove dispatch, A* pathfinding, Bresenham line-of-sight, BFS, all enumeration helpers (enumerateFloor/Rectangle/Line/Circle/Perimeter/Room), adjacency queries, cleared check

### Design Decisions
- GameModelRef: avoids circular imports — Entity/Floor/Tile read GameModelRef.main instead of importing GameModel directly
- Entity stores inlined into Floor.ts — StaticEntityGrid (2D array) for tiles/grasses/items/triggers, MovingEntityList (list + lazy grid cache) for bodies
- Entity type discrimination: uses duck-typing ('hp' in e → body, '_isGrass' in e → grass, etc.) since TS doesn't have C# `is` operator for our inheritance chain. Will refine when Body/Actor/Grass classes exist.
- PathfindingManager inlined as Floor.findPath() — A* with adjacentTiles (8-dir Moore neighborhood), weighted by tile/body collision
- FloorEnumeratorExtensions inlined as Floor methods — enumerateLine (Bresenham), enumerateCircle, BFS, etc.
- No uuid dependency — Entity guids use incrementing counter (`e-0`, `e-1`, ...) since we don't need persistence

## Sprint 3 (2026-02-25): Body + Actor + Player + Actions ✅
*Plan session 3*

### Completed Files
- [x] src/model/BaseAction.ts — BaseAction + MoveBaseAction, AttackBaseAction, AttackGroundBaseAction, WaitBaseAction, GenericBaseAction, StruggleBaseAction, ActionCosts, CannotPerformActionException
- [x] src/model/Body.ts — HP, movement (with collision/bodyLeft/bodyEntered), damage pipeline (takeAttackDamage, takeDamage, heal), modifier-driven movementLayer. Handler symbols: BODY_MOVE_HANDLER, BODY_TAKE_ATTACK_DAMAGE_HANDLER, TAKE_ANY_DAMAGE_HANDLER, HEAL_HANDLER
- [x] src/model/Actor.ts — tasks/taskQueue, StatusList, faction, Step() (full task lifecycle), Perform() (modifier chain), attack/attackGround, getActionCost with modifiers. Handler symbols: ATTACK_HANDLER, DEAL_ATTACK_DAMAGE_HANDLER, ACTION_PERFORMED_HANDLER, ACTOR_KILLED_HANDLER
- [x] src/model/Player.ts — 12 HP, Inventory, Equipment, visibility recompute on move, combat handlers, baseAttackDamage from weapon
- [x] src/model/StatusList.ts — stub with add/remove/has/get
- [x] src/model/ActorTask.ts — stub with ActorTask base + DoOnceTask + TaskStage enum
- [x] src/model/Inventory.ts — stub with maxSize, add/remove
- [x] src/model/Equipment.ts — stub with EquipmentSlot enum, get/set/remove

### Design Decisions
- Body._isBody = true for Floor type discrimination (duck typing)
- Handler interfaces use Symbol.for('HandlerName') for modifier collection
- Player implements handlers by setting symbol tags as properties + defining handler methods
- ActionCosts extends Map<ActionType, number> (simpler than C# Dictionary subclass)
- Actor.step() fully ported including task lifecycle, free tasks, modifier processing

## Sprint 4 (2026-02-25): TurnManager + GameModel + All Tasks ✅
*Plan session 4*

### Completed Files
- [x] src/model/ActorTask.ts — full implementation: ActorTask base + DoOnceTask, TaskStage, named/onlyCheckBefore/free fluent builders
- [x] src/model/tasks/WaitTask.ts
- [x] src/model/tasks/AttackTask.ts
- [x] src/model/tasks/FollowPathTask.ts — path following with maxMoves
- [x] src/model/tasks/MoveNextToTargetTask.ts — A* to adjacent tile, static findBestAdjacentPath
- [x] src/model/tasks/ChaseTargetTask.ts — recomputes path each step
- [x] src/model/tasks/ChaseDynamicTargetTask.ts — dynamic target via callback
- [x] src/model/tasks/MoveToTargetTask.ts + MoveToTargetThenPerformTask
- [x] src/model/tasks/MoveRandomlyTask.ts — static getRandomMove helper
- [x] src/model/tasks/RunAwayTask.ts — move away from fear point
- [x] src/model/tasks/SleepTask.ts + SurprisedStatus — sleep until visible, wake on damage
- [x] src/model/tasks/GenericTask.ts + GenericPlayerTask
- [x] src/model/tasks/TelegraphedTask.ts — countdown then perform action
- [x] src/model/tasks/AttackGroundTask.ts — extends TelegraphedTask
- [x] src/model/TurnManager.ts — FULLY SYNCHRONOUS step loop (no coroutines). Finds active entity by timeNextAction, handles NoActionException/CannotPerformActionException/ActorDiedException, fires timed events, drains event queue.
- [x] src/model/GameModel.ts — simplified single-floor: player, floor, time, turnManager, timedEvents, eventQueue/drainEventQueue, createTestGame for dev testing. Implements IGameModelRef for circular dep breaking.

### Stats
- 36 TypeScript files, 3,346 total lines
- tsc --noEmit passes clean

### Design Decisions
- TurnManager.stepUntilPlayerChoice() is fully synchronous (no yields/WaitForSeconds). Animations will be replayed separately by renderer.
- GameModel.createTestGame() creates a simple walled room for Phase 1 testing
- TelegraphedTask in tasks/ (not enemies/) since it's used by multiple enemy types
- SurprisedStatus defined inline with SleepTask (matches C# pattern where it lives in SleepTask.cs)

## Sprint 5 (2026-02-26): Rendering Layer (PixiJS) ✅
*Plan session 5*

### Completed Files
- [x] scripts/copy-sprites.js — copies ~193 PNGs from Assets/Textures/ to public/sprites/, generates manifest.json with dimensions/frame counts
- [x] src/renderer/SpriteManager.ts — loads manifest + textures via PixiJS Assets, slices multi-frame strips, name→sprite resolution
- [x] src/renderer/Camera.ts — tile↔pixel conversion, viewport fit/centering, pixelToTile for input
- [x] src/renderer/GameRenderer.ts — PixiJS Application with 6 layered containers (tiles, grasses, items, bodies, effects, fog), full rebuild + incremental sync, tile color backgrounds, entity sprites with fallback tinting
- [x] src/renderer/FogOverlay.ts — per-tile black overlay: Unexplored=opaque, Explored=60%, Visible=transparent
- [x] src/renderer/AnimationPlayer.ts — GSAP tweens for move (slide 120ms), attack (bump), damage (flash+shake), heal (green flash), death (shrink+fade), spawn (pop in), floating damage indicators
- [x] src/renderer/index.ts — barrel export
- [x] src/App.tsx — wired up: creates PixiJS app, loads sprites, creates test game, renders floor
- [x] Fixed GameModel.createTestGame() — replaced require('./Tile') with ESM import

### Stats
- 42 TypeScript files, 4,174 total lines
- tsc --noEmit passes clean
- npm run dev serves correctly (Vite dev server)

### Design Decisions
- Sprite PNGs copied at build time via `npm run sprites` (also runs before dev/build) — avoids committing duplicates
- public/sprites/ in .gitignore (generated files)
- Most sprites 16x16, some wider for animation frames (e.g., goo 80x16 = 5 frames). SpriteManager slices strips at 16px width.
- Tile rendering: colored rectangles per tile type (Ground=#8b7355, Wall=#3a3a4a, etc.) with optional sprite overlay
- No atlas packing yet — individual PNGs loaded directly, sufficient for ~200 small sprites
- Entity fallback: when sprite not found, white texture tinted with deterministic HSL from name hash
- AnimationPlayer uses GSAP timeline — events queued sequentially, skip() fast-forwards

## Sprint 6 (2026-02-26): Input + HUD + Wire-Up ✅
*Plan session 6*

### Completed Files
- [x] src/input/InputHandler.ts — keyboard (arrows, WASD, numpad, diagonals QEZC) + pointer click/tap → PlayerIntent events. Space/period/numpad5 = wait.
- [x] src/hooks/useGameLoop.ts — full game loop orchestration: creates model + renderer + input, resolveIntent() translates intents to tasks (FollowPathTask for movement, AttackTask for adjacent enemies, MoveNextToTargetTask for ranged click, WaitTask), steps model, syncs renderer, updates React state
- [x] src/ui/HUD.tsx — HP bar (color changes by threshold), turn counter, enemy count, dead/cleared status
- [x] src/App.tsx — simplified to useGameLoop + HUD
- [x] src/renderer/Camera.ts — fixed Y-axis flip (Unity Y-up → PixiJS Y-down)
- [x] src/model/GameModel.ts — fixed require('./Tile') → ESM import for Ground/Wall

### Stats
- 46 TypeScript files, ~4,500 total lines
- tsc --noEmit passes clean
- Player can move (WASD/arrows/numpad), wait (space), click tiles to pathfind

### Design Decisions
- Y-axis flipped in Camera (game y=0 at bottom of screen, matching Unity convention)
- InputHandler emits PlayerIntent union type (move/click/wait), useGameLoop resolves to tasks
- resolveIntent: directional move checks for enemy at target (→ AttackTask), click supports pathfinding + move-next-to-enemy
- Game loop is synchronous: set task → stepUntilPlayerChoice → syncToModel → update React state
- No animation playback yet (instant sync) — AnimationPlayer is wired but events not collected from model

## Sprint 7 (2026-02-26): AIActor + First Enemies ✅
*Plan session 8 (partial — 3 of 6 enemies, skipped session 7 statuses/items)*

### Completed Files
- [x] src/model/enemies/AIActor.ts — base class: retry loop on NoActionException, death handler, sleep-on-spawn
- [x] src/model/enemies/Blob.ts — Blob (6 HP, telegraphed attack) + MiniBlob (3 HP)
- [x] src/model/enemies/Bird.ts — Bird (3 HP, flying, jumps distance-2 tiles, waits after jump)
- [x] src/model/enemies/Snake.ts — Snake (3 HP, only moves in same row/col as player, applies weakness stub)
- [x] src/model/tasks/JumpToTargetTask.ts — instant teleport/jump move (used by Bird)
- [x] src/model/enemies/index.ts — barrel export
- [x] GameModel.createTestGame updated: 14x10 room with Blob, Bird, Snake placed

### Design Decisions
- AIActor.step() wraps super.step() with retry loop (up to 2 retries on NoActionException)
- All enemies start with SleepTask (wake when player is visible)
- Bird.getJumpTiles uses diamond distance == 2 filter on enumerateCircle
- Snake.handleDealAttackDamage is stubbed (WeaknessStatus not yet ported)
- turnPriority overrides: Blob→90 during AttackGroundTask, Snake→20 always

## Sprint 8 (2026-02-26): Floor Generator + Tile Sprites ✅
*Plan sessions 11+12 (jumped ahead — floor gen + encounters in one sprint)*

### Completed Files (13 new generator files)
- [x] src/generator/WeightedRandomBag.ts — weighted random selection with discount/subtract mechanics
- [x] src/generator/Room.ts — BSP tree: randomlySplit, randomlyShrink, traverse, aspect-ratio-weighted splits
- [x] src/generator/ShapeTransform.ts — 3×3 pattern matching with 4 rotations, 3 predefined transforms
- [x] src/generator/TileGroup.ts — BFS flood-fill: makeGroupAndRemove, partitionIntoDisjointGroups, getCenterTile
- [x] src/generator/TileSection.ts — ASCII art tile patterns: fromString, blit, rot90, withRotations
- [x] src/generator/TileSectionConcavity.ts — 15 concavity patterns × 4 rotations
- [x] src/generator/FloorUtils.ts — tidyUpAroundStairs, emptyTilesInRoom, tilesFrom/AwayFrom, line3x3, surroundWithWalls, carveGround, clusters, naturalizeEdges
- [x] src/generator/entityRegistry.ts — name→constructor map, create() returns null for unregistered
- [x] src/generator/EncounterGroup.ts — EncounterBag, EncounterGroup (8 bags), createEncounterGroups with exact C# weights for 3 difficulty tiers
- [x] src/generator/Encounters.ts — all encounter functions (~500 lines): structural, chasm, water, enemy, grass, plant, item, NPC encounters
- [x] src/generator/FloorGenerator.ts — 28 depth-specific generators, retry pipeline, ensureConnectedness, BSP multi-room
- [x] src/generator/Tips.ts — depth→tip text map
- [x] src/generator/index.ts — barrel export

### Modified Files
- [x] src/core/Vector2Int.ts — added static min/max methods
- [x] src/model/Floor.ts — added upstairsRoom, downstairsRoom, downstairsPos properties
- [x] src/model/GameModel.ts — added createDailyGame(seed), selectDepth(), djb2 hash
- [x] src/model/enemies/Blob.ts, Bird.ts, Snake.ts — entityRegistry.register() calls
- [x] src/hooks/useGameLoop.ts — switched to createDailyGame(), added R key for dev regeneration
- [x] src/renderer/SpriteManager.ts — tilesheet slicing (tiles0/12/24 → ground/wall/fancy-ground/upstairs/downstairs sub-sprites), getTileTexture(type, depth)
- [x] src/renderer/GameRenderer.ts — depth-aware tile rendering using tilesheet sprites, fallback to individual sprites then colored rectangles

### Stats
- ~65 TypeScript files, ~7,500 lines
- tsc --noEmit passes clean, vite build succeeds

### Design Decisions
- entityRegistry pattern: encounters reference entities by name string, create() returns null for unported types → encounters silently no-op
- createEncounterGroups(E) takes encounter map as parameter to break circular dependency
- postProcessFloor does direct cleared check (not floor.checkCleared()) since GameModel doesn't exist during generation
- selectDepth() is a standalone function, easy to swap for weekly/monthly difficulty curves
- Tilesheet sprites sliced from Unity .meta coordinates: ground(0,16), wall(16,16), fancy-ground(32,16), upstairs(0,32), downstairs(16,32)
- Depth → tilesheet: 0-9 → tiles0, 10-18 → tiles12, 19+ → tiles24 (matches C# DeepLevelSpriteSwap)
- R key regeneration only in dev mode (import.meta.env.DEV)

## Sprint 9 (2026-02-26): Status System + Items + Equipment + Grass Base ✅
*Plan session 7 (was skipped — sprints 7-8 jumped ahead to enemies + floor gen)*

### Completed Files (new)
- [x] src/model/Status.ts — Status abstract base + StackingStatus + StackingMode enum. Implements IStepModifier (STEP_MOD). Remove() via GameModelRef.enqueuEvent for safe iteration.
- [x] src/model/Item.ts — Item base class (NOT Entity), EquippableItem abstract, all interfaces: IWeapon, IDurable, IStackable, IConditionallyStackable, IUsable, IEdible, IPlantable, ISticky + symbol tags + helper functions (reduceDurability, increaseDurability, mergeStacks)
- [x] src/model/ItemOnGround.ts — Entity wrapper for floor-placed items, IActorEnterHandler for auto-pickup, PlacementBehavior BFS
- [x] src/model/grasses/Grass.ts — Entity subclass, immobile pos, bodyModifier, onNoteworthyAction callback, _isGrass marker
- [x] src/model/items/ItemHands.ts — EquippableItem + IWeapon, attackSpread [1,1], inventory getter returns player.equipment
- [x] src/model/items/ItemStick.ts — EquippableItem + IDurable + IWeapon, durability 3, attackSpread [2,2]
- [x] src/model/items/ItemRedberry.ts — Item + IStackable + IUsable, stacksMax 10, heals 2 HP, self-destroys at 0 stacks
- [x] src/model/items/index.ts — barrel export
- [x] src/model/grasses/index.ts — barrel export
- [x] src/model/statuses/index.ts — placeholder barrel export

### Modified Files
- [x] src/model/StatusList.ts — replaced stub: imports Status from Status.ts, add fires IStatusAddedHandler/IStatusRemovedHandler via collectModifiers, status.list lifecycle (Start/End), removeOfType(), findOfType()
- [x] src/model/Inventory.ts — replaced stub: fixed-size Item[] array, IStackable merging, slot swapping, tryDropItem creates ItemOnGround, EventEmitter events (onItemAdded/Removed/Destroyed), hasItem, indexOf, itemsNonNull iterator
- [x] src/model/Equipment.ts — replaced stub: fixed EquipmentSlot enum (Headwear=0, Weapon=1, Armor=2, Offhand=3, Footwear=4 matching C#), extends Inventory, addItem only accepts EquippableItem, ISticky check, weapon slot returns Player.hands if empty, OnEquipped/OnUnequipped callbacks
- [x] src/model/Player.ts — added ItemHands import, hands field, typed IWeapon/IDurable usage in onAttack/baseAttackDamage
- [x] src/model/tasks/SleepTask.ts — SurprisedStatus updated to extend new Status base class, uses Status.Remove() and Consume(), handles dual STEP_MOD+BASE_ACTION_MOD via instanceof check

### Stats
- 75 TypeScript files, ~8,600 lines
- tsc --noEmit passes clean, vite build succeeds

### Design Decisions
- Item is NOT an Entity (matches C#). ItemOnGround wraps Item as Entity for floor placement.
- EquipmentSlot enum fixed to match C# ordering (was wrong in stub)
- Status.modify() handles STEP_MOD (calls Step(), returns input). Subclasses needing other modifier tags (like SurprisedStatus with BASE_ACTION_MOD) use instanceof BaseAction to distinguish calls.
- Symbol tags for all item interfaces (WEAPON_TAG, DURABLE_TAG, etc.) following established pattern
- Equipment extends Inventory (C# pattern) — shared addItem/removeItem with specialized slot logic
- Circular dep Item↔Equipment broken by `import type { EquipmentSlot }` in Item.ts
- ItemOnGround._isItem marker for Floor duck typing (matches _isGrass, _isBody pattern)

## Sprint 10 (2026-02-26): Remaining Session 8 — Enemies + Statuses + Grasses + Items ✅
*Plan session 8 (remainder — 6 enemies, 8 statuses, 4 grasses, 3 items)*

### Prerequisite Fix
- [x] Tile.bodyEntered()/bodyLeft() — replaced TODO stubs with collectModifiers dispatch using centralized ACTOR_ENTER_HANDLER/ACTOR_LEAVE_HANDLER symbols (moved from local defs to types.ts)

### New Statuses (8 files in `src/model/statuses/`)
- [x] WeaknessStatus.ts — StackingStatus + IAttackDamageModifier. Reduces attack damage by 1/stack.
- [x] PoisonedStatus.ts — StackingStatus, Independent stacking. Timed event ticks: 3+ stacks → 3 damage + remove 3; else lose 1 stack per 5 turns.
- [x] WebbedStatus.ts — Status + ATTACK_DAMAGE_TAKEN_MOD + BASE_ACTION_MOD + DEATH_HANDLER. Blocks movement (→ StruggleBaseAction), +1 damage taken. Duck-typed Web check via `_isWeb` marker.
- [x] InShellStatus.ts — StackingStatus, Max mode, 4 stacks. Forces WAIT action, reduces damage by 1.
- [x] SlimedStatus.ts — Status + ACTION_COST_MOD + BASE_ACTION_MOD. Doubles MOVE cost, removes on MOVE.
- [x] SoftGrassStatus.ts — StackingStatus, Ignore mode + BODY_MOVE_HANDLER. Tracks consecutive SoftGrass moves, grants FreeMoveStatus at 2.
- [x] FreeMoveStatus.ts — StackingStatus + ACTION_COST_MOD + ACTION_PERFORMED_HANDLER. Sets MOVE cost to 0, consumed on MOVE.
- [x] GuardedStatus.ts — StackingStatus + ATTACK_DAMAGE_TAKEN_MOD. Delegates stacks to Guardleaf.guardLeft, absorbs damage.

### New Grasses (4 files in `src/model/grasses/`)
- [x] Web.ts — Grass + IActorEnterHandler. Applies WebbedStatus. `_isWeb` marker for duck typing. Static `canOccupy`, `isActorNice`.
- [x] Bladegrass.ts — Grass + IActorEnterHandler + IActorLeaveHandler. Sharpens on leave, deals 2 damage on enter when sharp. Self-destructs after 10 turns once sharpened.
- [x] SoftGrass.ts — Grass + IActorEnterHandler. Adds SoftGrassStatus to player on enter.
- [x] Guardleaf.ts — Grass + IActorEnterHandler. guardLeft=5. Adds/removes GuardedStatus. removeGuard() decrements pool, self-kills at 0.

### New Items (3 files in `src/model/items/`)
- [x] ItemBatTooth.ts — EquippableItem + IWeapon + IDealAttackDamageHandler. Weapon slot, [1,1]. Heals player by damage dealt, self-destructs.
- [x] ItemSpiderSandals.ts — EquippableItem + IStackable + IBodyMoveHandler. Footwear slot, stacksMax 999. Leaves Web trail on move.
- [x] ItemSnailShell.ts — Item + IStackable. stacksMax 3. Throw mechanic deferred (needs targeting UI).

### New Enemies (6 files in `src/model/enemies/`)
- [x] Scorpion.ts — 4 HP, [1,1]. MOVE=0.5, ATTACK=0.5 (acts twice per turn). Chase+attack or moveRandomly.
- [x] Crab.ts — 5 HP, [2,2], Neutral faction, turnPriority=40. Horizontal-only movement, flips direction on block.
- [x] Bat.ts — 5 HP, [1,1], Flying. Heals 1 on damage dealt, deep sleep after 5 turns awake.
- [x] Goo.ts — 12 HP, [1,1], turnPriority=50. Splits into 2 goos (ceil/floor HP) when damaged.
- [x] Snail.ts — 3 HP, [2,2]. Pauses after each action (WaitTask). InShellStatus on damage.
- [x] Spider.ts — 4 HP, [0,0]. Applies PoisonedStatus on hit. AI: place webs, then attack, else move to non-webbed tile.

### Modified Files
- [x] src/core/types.ts — added ACTOR_ENTER_HANDLER, ACTOR_LEAVE_HANDLER symbols + IActorLeaveHandler interface
- [x] src/core/index.ts — barrel exports for new symbols
- [x] src/model/Tile.ts — bodyEntered/bodyLeft now dispatch via collectModifiers
- [x] src/model/ItemOnGround.ts — uses centralized ACTOR_ENTER_HANDLER from types.ts
- [x] src/model/enemies/Snake.ts — replaced WeaknessStatus stub with real import + implementation
- [x] src/model/enemies/index.ts — added all 6 new enemy exports
- [x] src/model/statuses/index.ts — added all 8 new status exports
- [x] src/model/items/index.ts — added 3 new item exports
- [x] src/model/grasses/index.ts — added 4 new grass exports
- [x] src/model/GameModel.ts — side-effect imports for all new entity registrations
- [x] src/renderer/SpriteManager.ts — NAME_MAP entries for soft grass, guardleaf, bat tooth, spider sandals, snail shell

### Stats
- ~96 TypeScript files, ~10,300 lines (est)
- tsc --noEmit passes clean, vite dev server runs

### Design Decisions
- **Multi-modifier modify() pattern**: Statuses implementing STEP_MOD (inherited) + another modifier use `modify(input: any): any` with runtime type checking: `typeof input === 'number'` for damage mods, `input instanceof BaseAction` for action mods, default → `super.modify(input)` for STEP_MOD chain.
- **Duck typing for circular deps**: WebbedStatus ↔ Web circular dep broken via `_isWeb` property marker. `isActorWebNice()` checks constructor names instead of instanceof.
- **Entity registration via side-effect imports**: Each entity file calls `entityRegistry.register('Name', Class)` at module bottom. GameModel.ts imports them to trigger registration.
- **Goo split**: Creates 2 new Goos at same position with ceil/floor(hp/2), adds SurprisedStatus, kills self via floor.remove().

## Sprint 11 (2026-02-26): Combat Animations + Visual Feedback ✅
*Plan session 9 (combat animations + status indicators)*

### Modified Files (no new files)
- [x] src/model/TurnManager.ts — removed unused GameEvent interface (conflicted with AnimationPlayer's)
- [x] src/model/GameModelRef.ts — added emitAnimation(event) to IGameModelRef interface
- [x] src/model/GameModel.ts — added animationEvents array, emitAnimation(), consumeAnimationEvents()
- [x] src/model/Body.ts — emit animation events for move (set pos), takeDamage, heal, kill
- [x] src/model/Actor.ts — emit animation events for attack (attackWithDamage), attackGround
- [x] src/hooks/useGameLoop.ts — wire animation playback via animator.play(), skip support (any key/click)
- [x] src/renderer/AnimationPlayer.ts — replace red circle damage numbers with Text ("-N" red, "+N" green), add heal numbers
- [x] src/renderer/GameRenderer.ts — add status indicator dots (colored circles below entity sprites), STATUS_COLORS map

### Stats
- ~96 TypeScript files, ~10,400 lines (est)
- tsc --noEmit passes clean, pre-existing build warnings unchanged

### Design Decisions
- **Animation event pipeline**: Model emits events → GameModel.animationEvents array → consumed by useGameLoop → AnimationPlayer.play(). Guards with GameModelRef.mainOrNull so floor generation (null ref) doesn't crash.
- **emitAnimation(event: object)** on IGameModelRef uses `object` type to avoid model→renderer circular dep. GameModel casts to GameEvent internally.
- **Skip support**: One-shot keydown/pointerdown listeners during animation playback call animator.skip(). Removed in finally block.
- **Damage/heal numbers**: PixiJS Text with TextStyle (monospace, bold, 14px, black stroke). "-N" red for damage, "+N" green for heal. Float up 24px and fade over 0.6s.
- **Status indicators**: Colored dots drawn below entity sprites using Graphics.circle(). Rebuilt each syncEntities() call. STATUS_COLORS maps constructor name → hex color (9 statuses mapped).
- **No new files**: All changes integrated into existing renderer/model files.

## Sprint 12 (2026-02-27): Inventory UI + Game Over Detection ✅
*Plan session 10 (Phase 2 gate)*

### New Files (3)
- [x] src/ui/StatusBar.tsx — horizontal row of status pills (colored left-border + name + stacks), STATUS_COLORS map
- [x] src/ui/InventoryPanel.tsx — bottom-anchored panel: 5 equipment slots (labeled Head/Weapon/Armor/Off/Feet) + 10 inventory slots. Click slot → action popup with item stats + method buttons (Drop/Eat/Use/Equip/Unequip). Internal selected-slot state.
- [x] src/ui/GameOverOverlay.tsx — fullscreen semi-transparent overlay: win ("Floor Cleared!") or loss ("You perished to {killedBy}") + stats table (turns, enemies, damage dealt/taken) + Play Again button

### Modified Files
- [x] src/model/GameModelRef.ts — added gameOver(), floorCleared(), stats to IGameModelRef interface
- [x] src/model/Player.ts — handleDeath now calls gameOver(false, source); handleTakeAnyDamage tracks damageTaken; handleDealAttackDamage tracks damageDealt; onKill tracks enemiesDefeated
- [x] src/model/Floor.ts — clearFloor() calls gameOver(true) + floorCleared(), guarded by mainOrNull for gen-time safety
- [x] src/hooks/useGameLoop.ts — expanded GameState with ItemSnapshot[], StatusSnapshot[], GameOverInfo. Added snapshotItem helper, stepAndAnimate extracted from processIntent. New callbacks: executeItemAction (dispatches Drop/Eat/Use/Equip/Unequip, costs turn for Drop/Eat/Use), resetGame (daily seed). processIntent now blocks on isCleared too.
- [x] src/ui/HUD.tsx — integrated StatusBar, removed inline DEAD/CLEARED spans (GameOverOverlay handles those)
- [x] src/App.tsx — composes InventoryPanel + GameOverOverlay

### Stats
- ~99 TypeScript files, ~10,800 lines (est)
- tsc --noEmit passes clean, vite build succeeds

### Design Decisions
- **Snapshot-based state**: GameState contains plain serializable snapshots (ItemSnapshot, StatusSnapshot, GameOverInfo) — no model refs leak into React. readState() creates fresh snapshots after every step.
- **ItemHands filtered**: Equipment.getAt(Weapon) falls back to Player.hands; snapshot code filters this out by constructor name check to avoid showing "Hands" in UI.
- **Turn-costing actions**: Drop/Eat/Use cost a turn (player gets a WaitTask + step). Equip/Unequip are free (just sync renderer).
- **clearFloor guarded**: Uses GameModelRef.mainOrNull since clearFloor() can be called during generation when main is null.
- **No drag-and-drop**: Simplified from C# version. Click slot → popup → click action button.

## Sprint 13 (2026-02-28): Bulk Content Porting — Batch 1 ✅
*Plan sessions 13-15 (content porting batch 1)*

### New Infrastructure Files (2)
- [x] src/model/enemies/Destructible.ts — Destructible base (all damage forced to 1) + Rubble (IBlocksVision), Stump, Stalk (chain-kills all Stalks on death)
- [x] src/model/enemies/SimpleStatusApplicationEnemy.ts — Abstract AIActor subclass for cooldown-based status application (used by Healer, Poisoner, Muckola)

### New Enemy Files (20)
- [x] src/model/enemies/Wallflower.ts — wall-tethered, ConfusedStatus on attack
- [x] src/model/enemies/HardShell.ts — blocks 3+ damage
- [x] src/model/enemies/Golem.ts — 2x action cost, leaves Rubble on death
- [x] src/model/enemies/Dizapper.ts — applies VulnerableStatus
- [x] src/model/enemies/Bloodstone.ts — BloodstoneStatus on all actors on floor
- [x] src/model/enemies/Butterfly.ts — flying, flees player
- [x] src/model/enemies/Skully.ts — basic enemy + co-located Muck grass class (circular dep: Muck↔Skully)
- [x] src/model/enemies/Thistlebog.ts — basic enemy
- [x] src/model/enemies/Healer.ts — heals allies, extends SimpleStatusApplicationEnemy
- [x] src/model/enemies/Poisoner.ts — SimpleStatusApplicationEnemy, applies PoisonedStatus
- [x] src/model/enemies/Muckola.ts — SimpleStatusApplicationEnemy, spawns Muck tile
- [x] src/model/enemies/Hopper.ts — eats grass to heal, jumps over entities
- [x] src/model/enemies/Clumpshroom.ts — ClumpedLungStatus on attack
- [x] src/model/enemies/Wildekin.ts — charges player, stuns on wall collision
- [x] src/model/enemies/Scuttler.ts — transforms to/from ScuttlerUnderground grass
- [x] src/model/enemies/Jackal.ts — pack hunter + JackalBoss (16 HP)
- [x] src/model/enemies/Parasite.ts — Parasite + ParasiteEgg
- [x] src/model/enemies/Octopus.ts — ranged ink attack
- [x] src/model/enemies/IronJelly.ts — iron shell damage reduction

### New Grass Files (10)
- [x] src/model/grasses/Brambles.ts — 1 damage on enter
- [x] src/model/grasses/Mushroom.ts — harvest ItemMushroom
- [x] src/model/grasses/Astoria.ts — heal 4 HP or pick up ItemAstoria
- [x] src/model/grasses/EveningBells.ts — sleep non-player actors
- [x] src/model/grasses/Bloodwort.ts — gain StrengthStatus
- [x] src/model/grasses/Redcap.ts — confuse adjacent enemies
- [x] src/model/grasses/Violets.ts — open/close cycle, PacifiedStatus
- [x] src/model/grasses/Llaora.ts — disperse confuses enemies
- [x] src/model/grasses/Poisonmoss.ts — applies Poison, spreads
- [x] src/model/grasses/Fern.ts — vision blocker
- [x] src/model/grasses/Muck.ts — re-export from Skully.ts

### New Item Files (5)
- [x] src/model/items/ItemMushroom.ts — stackable consumable, PumpedUpStatus
- [x] src/model/items/ItemPumpkinHelmet.ts — durable headwear, blocks 1 damage
- [x] src/model/items/ItemWildwoodLeaf.ts — stackable consumable, StatusWild
- [x] src/model/items/ItemAstoria.ts — usable consumable, heal 4
- [x] src/model/items/ItemCharmBerry.ts — stackable, charms enemies

### New Status Files (11)
- [x] src/model/statuses/VulnerableStatus.ts — +1 attack damage taken per stack
- [x] src/model/statuses/BloodstoneStatus.ts — +1 dealt/taken, linked to Bloodstone enemy
- [x] src/model/statuses/ConfusedStatus.ts — random movement for stacks turns
- [x] src/model/statuses/StrengthStatus.ts — +1 attack per stack, consumed per attack
- [x] src/model/statuses/PacifiedStatus.ts — can't attack, removes near open Violet
- [x] src/model/statuses/RecoveringStatus.ts — heals after 25 turns
- [x] src/model/statuses/CharmedStatus.ts — enemies become friendly
- [x] src/model/statuses/PumpedUpStatus.ts — attack boost from mushroom
- [x] src/model/statuses/StatusWild.ts — wildwood leaf effect
- [x] src/model/statuses/ClumpedLungStatus.ts — debuff from Clumpshroom
- [x] src/model/statuses/ParasiteStatus.ts — spawns ParasiteEgg on death

### Modified Files
- [x] src/model/Body.ts — removed [key: symbol]: true index signatures from handler interfaces
- [x] src/model/GameModel.ts — 26 new side-effect imports for entity registration
- [x] src/model/GameModelRef.ts — minor updates
- [x] src/model/Item.ts — inventory field → getter/setter pair (allows subclass override)
- [x] src/model/ItemOnGround.ts — renamed item → heldItem (avoids Entity.item clash), fixed BFS import
- [x] src/model/Tile.ts — added FungalWall class + entityRegistry registration
- [x] src/model/enemies/AIActor.ts — minor fixes
- [x] src/model/enemies/Bat.ts, Crab.ts, Scorpion.ts, Snail.ts — import fixes
- [x] src/model/enemies/index.ts — added all new enemy exports
- [x] src/model/grasses/index.ts — added all new grass exports
- [x] src/model/items/index.ts — added new item exports
- [x] src/model/statuses/index.ts — added all new status exports
- [x] src/model/statuses/FreeMoveStatus.ts, GuardedStatus.ts — minor fixes
- [x] src/renderer/SpriteManager.ts — NAME_MAP entries for new entities
- [x] src/renderer/GameRenderer.ts — STATUS_SPRITES + TILE_COLORS for new statuses/tiles

### Stats
- 148 TypeScript files, ~13,500 lines
- tsc --noEmit passes clean
- ~50 new files added this sprint

### Design Decisions
- **Muck↔Skully circular dep**: Muck co-located in Skully.ts (Muck spawns Skully on step, Skully spawns Muck on death). grasses/Muck.ts re-exports from enemies/Skully.ts.
- **ItemOnGround.heldItem rename**: Entity base class has an `item` getter that returns the grass/trigger at the entity's position. ItemOnGround's wrapped item renamed to `heldItem` to avoid clash.
- **Handler interface cleanup**: Removed `[key: symbol]: true` index signatures from Body.ts handler interfaces — they caused "incorrectly implements interface" errors when classes had additional symbol-tagged properties.
- **Parallel agent strategy**: 5 worktree agents ported content simultaneously (enemies×3, grasses×1, items×1), then integrated into main branch.

### Remaining Undone Plan Sessions
- Sessions 13-16: Remaining complex content (HydraHeart, Grasper, Shielder, FruitingBody, Plants, remaining grasses)
- Session 17: Daily Puzzle UI + Scoring
- Session 18: Deployment + Final Polish
